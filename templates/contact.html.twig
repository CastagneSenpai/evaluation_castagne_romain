{% extends 'base.html.twig' %}

{% block body %}

    {% for product in products %}
        <h2>{{ contact.id }}</h2>
        <h2>{{ contact.nom }}</h2>
    {% endfor %}
    <br><br>
    <p>--------------------------------------------------</p>
    <br><br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form)

{% endblock %}